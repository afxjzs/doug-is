/**
 * This layout overrides the admin layout for the register page
 * to provide a clean registration interface without the admin navigation.
 */

export default function RegisterLayout({
	children,
}: {
	children: React.ReactNode
}) {
	return children
}
